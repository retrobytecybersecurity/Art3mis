# ══════════════════════════════════════════════════════════════
# Artemis — Python Package Requirements
# Install: pip3 install -r requirements.txt --break-system-packages
# ══════════════════════════════════════════════════════════════

# ── Web server ────────────────────────────────────────────────
flask>=3.0.0              # Web framework

# ── Report generation ─────────────────────────────────────────
reportlab>=4.0.0          # PDF reports
python-docx>=1.1.0        # Word (.docx) reports
openpyxl>=3.1.0           # Excel (.xlsx) export — assessment summary

# ── Scanning tools (Python-based) ────────────────────────────
pymeta3                   # File metadata OSINT
bbot                      # OSINT framework (passive + active)

# ── Auth ──────────────────────────────────────────────────────
bcrypt                    # Password hashing

# ── NOTE: The following tools are NOT pip packages ────────────
#
#   apt:  nmap, nikto, sslscan, dnsenum, curl, theharvester, wpscan
#   go:   nuclei, ffuf, assetfinder, gowitness, termshot, amass
#   git:  shcheck    → /opt/shcheck
#         spoofy     → /opt/spoofy
#         o365spray  → /opt/o365spray
#   manual: metasploit → https://docs.metasploit.com
